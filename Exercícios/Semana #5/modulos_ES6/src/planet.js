/* 
    como so temos essa classe planet, nao precisamos fazer nomeado, então,
    faremos export default
*/
export default class Planet {
    constructor(name, auToSun) {
        this.name = name
        this.distAuToSun = auToSun
    }
}